# Default di progetto per gli script di hardening LXC
# Valori NON segreti, letti come default dagli script (override via variabile d'ambiente).
# NB: NON inserire qui valori session-only (RELAY_HOST, INTERNAL_NET) né password.

# Destinatario delle notifiche di sicurezza (fail2ban, unattended-upgrades, cron)
ADMIN_EMAIL="alert@simoneradaelli.it"
